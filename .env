# --- Application Configuration ---
# Ensure this matches the user, password, host (db service name), port, and db name below
DATABASE_URL=postgresql://fahrzua_user:mein_super_sicheres_passwort@db:5432/fahrzua_db

# Generate a strong, random secret for session encryption (e.g., using openssl rand -hex 32)
# IMPORTANT: Change this to a unique secret!
SESSION_SECRET=ein_sehr_geheimer_zufälliger_string_bitte_ändern

# Set Node environment ('development' or 'production')
NODE_ENV=development

# Path inside the container where media files are stored/accessed
MEDIA_PATH=/media

# --- PostgreSQL Database Configuration ---
# IMPORTANT: Change the password to something strong and unique!
POSTGRES_USER=fahrzua_user
POSTGRES_PASSWORD=mein_super_sicheres_passwort
POSTGRES_DB=fahrzua_db

# Optional: Set PostgreSQL Port if different from default 5432 (rarely needed)
# POSTGRES_PORT=5432
